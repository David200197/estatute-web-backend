import { Entity } from '@src/common/abstracts/entity.abstract';
import { <%= name.pascalCase %>Model, <%= name.pascalCase %>Options } from '../models/<%= name.kebabCase %>.model';
import { UuidObjectValue } from '@src/common/object-value/uuid.object-value';

export class <%= name.pascalCase %> extends Entity implements <%= name.pascalCase %>Model {
    public readonly uuid: string;
    constructor(options: <%= name.pascalCase %>Options) {
        super();
        this.uuid = new UuidObjectValue(options.uuid).value;
    }
}
