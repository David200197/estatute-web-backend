import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { <%= name.pascalCase %>Command } from './<%= name.kebabCase %>.command';
import { <%= name.pascalCase %>HandlerModel } from './<%= name.kebabCase %>-handler.model';

@CommandHandler(<%= name.pascalCase %>Command)
export class <%= name.pascalCase %>Handler
  implements <%= name.pascalCase %>HandlerModel, ICommandHandler<<%= name.pascalCase %>Command>
{
  execute({ }: <%= name.pascalCase %>Command): Promise<any> { 
    throw new Error('not implements')  
  }
}
